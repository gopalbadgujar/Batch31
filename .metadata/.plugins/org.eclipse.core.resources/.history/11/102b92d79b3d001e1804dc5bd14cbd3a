package com.set;

import java.util.LinkedHashSet;
import java.util.Set;

public class SetTypes {

	public static void main(String[] args) {
		
		//LinkedHashSet
		/*
		 * Null value print at only once
		 * avoid duplicate value in primitive data types 
		 * by helping hashCode & equals methods avoid duplicate value
		 *  
		 */
		Set<Integer> set = new LinkedHashSet<Integer>();
		set.add(201);
		set.add(202);
		set.add(203);
		set.add(201);
		set.add(601);
		set.add(201);
		set.add(null);
		set.add(null);
	
		System.out.println(set);

		
		
	}
	
	
}
